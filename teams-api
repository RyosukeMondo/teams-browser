#!/usr/bin/env bash
# Convenience wrapper: ./teams-api   (identical to ./teams serve)
here="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$here/.venv/bin/python" -X utf8 -m teams_browser serve "$@"
